answer <- 3 + 5
# <- is 'assignment operator'

ans <- sqrt(4) + 5

# Vector of heights
Height <- c(68, 62, 70, 74, 65)

# character data for gender - double or single quotes
Gender <- c('F', 'F', 'M', 'M', 'M')

true <- TRUE
false <- FALSE

# Passing (whether student is passing the class)
Passing <- c(TRUE, TRUE, TRUE, TRUE, FALSE, TRUE)

mean(Height)
median(Height)
sqrt(Height)
sqrt(Passing)
